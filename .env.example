# OpenAI API 配置示例
# 复制此文件为 .env 并填入你的实际配置
# 注意：.env 文件已被 .gitignore 忽略，不会被提交到仓库

# OpenAI API 密钥（必需）
OPENAI_API_KEY=your-api-key-here

# OpenAI API 基础 URL（可选，用于代理或兼容服务）
# 如果使用官方 OpenAI API，可以留空或设置为 https://api.openai.com/v1
# 如果使用代理或兼容服务，设置对应的 URL，例如：
# OPENAI_BASE_URL=http://172.17.0.1:3000/v1

# 自定义配置文件路径（可选）
# SRS_GEN2_CONFIG=/path/to/custom-config.yaml
